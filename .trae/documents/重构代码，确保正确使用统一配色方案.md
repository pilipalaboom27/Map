## 重构代码，确保正确使用统一配色方案

### 1. 分析当前问题
- CSS变量不生效，可能是样式加载顺序、组件库样式覆盖或浏览器缓存问题
- 组件没有正确使用CSS变量，导致样式不一致
- 代码结构不够清晰，难以维护

### 2. 重构计划
- **检查样式加载顺序**：确保CSS变量在所有样式之前定义
- **优化CSS变量定义**：确保变量定义在根元素上，具有全局作用域
- **重构组件样式**：确保所有组件正确使用CSS变量
- **避免样式冲突**：使用Scoped CSS和CSS变量来避免样式覆盖
- **添加调试代码**：帮助诊断样式问题

### 3. 具体实施步骤
1. **检查main.js**：确保样式文件加载顺序正确
2. **优化variables.css**：确保CSS变量定义在根元素上
3. **重构App.vue**：确保正确使用CSS变量
4. **重构GraphCanvas.vue**：确保正确使用CSS变量
5. **重构EChartsGraph.vue**：使用更可靠的方式应用CSS变量
6. **重构其他组件**：确保所有组件正确使用CSS变量
7. **测试和验证**：确保所有样式都能正确应用

### 4. 预期效果
- 统一的配色方案，所有组件使用相同的颜色
- 清晰的代码结构，易于维护
- 避免样式冲突，确保样式正确应用
- 良好的可扩展性，便于后续调整配色方案